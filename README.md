# credit-card-fraud
An end-to-end Python ML pipeline detecting fraud in 284k+ highly skewed transactions. It compares 7 classifiers, with Random Forest leading at 99.96% accuracy. The project addresses extreme class imbalance (0.17% fraud) via Confusion Matrix analysis and resolves hardware bottlenecks using parallel thread execution (n_jobs=-1).
